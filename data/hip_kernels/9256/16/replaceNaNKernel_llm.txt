```c
// Perform loop unrolling for better memory access patterns
// Ensure coalesced memory access by aligning threads with data layout
// Minimize divergence by handling NaN checks uniformly across threads
// Use shared memory if possible to reduce global memory access latency
```