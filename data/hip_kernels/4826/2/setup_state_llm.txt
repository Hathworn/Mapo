```cpp
// Ensure coalesced memory access by using contiguous thread index
// Avoid bank conflicts when accessing shared memory
// Minimize divergence by aligning thread execution paths
// Prefetch global data into shared memory for faster access
```