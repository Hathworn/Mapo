// Use grid-stride loop for better utilization
// Optimize memory accesses by coalescing
// Avoid branching inside the kernel if possible
// Use shared memory to cache frequently accessed data
// Minimize global memory access latency