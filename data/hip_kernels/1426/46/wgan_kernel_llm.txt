// Consider using shared memory to reduce global memory access for large arrays
// Use constant memory for frequently accessed data like truth values if they fit in constant memory space
// Explore memory coalescing techniques to ensure threads access contiguous memory locations