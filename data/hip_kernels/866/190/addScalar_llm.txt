```cpp
// Optimize memory access patterns for coalesced access
// Ensure the kernel runs with optimal block size for maximum warp occupancy
// Consider using shared memory if in-place processing can benefit from locality
```