```cpp
// Consider coalesced memory access patterns for qss and IN arrays
// Use shared memory if multiple threads access the same memory location
// Ensure to avoid bank conflicts when using shared memory
```