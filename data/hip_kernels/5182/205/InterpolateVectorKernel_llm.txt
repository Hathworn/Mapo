```
// Simplify thread ID calculation
// Ensure bounds checking before memory access for safety
// Cache repeated memory accesses to qVal and fVal to reduce global memory access
// Use 0.50f for floating-point consistency in arithmetic operations
```