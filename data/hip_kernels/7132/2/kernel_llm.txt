// Use grid stride for efficient looping to maximize memory throughput
// Precompute row and column indices to reduce repetitive calculations
// Avoid modulus operation by simple comparison to improve efficiency
// Reduce memory access by calculating indices once and reusing them