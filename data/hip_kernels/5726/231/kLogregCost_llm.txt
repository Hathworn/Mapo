```
// Use blockDim.x for better flexibility and grid size calculation
// Use static_cast for type safety
// Use __ldg for faster memory access
// Use __ldg inside the loop for memory access optimization
// Static cast for consistent type
```