```cpp
// Use shared memory if accessing the same data multiple times by multiple threads
// Contiguous memory accesses for coalesced access patterns
// Minimize conditional statements in the core computation
```