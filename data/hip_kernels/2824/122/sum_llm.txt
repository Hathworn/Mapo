```
// Consider using shared memory if data reuse is possible
// Coalesce memory accesses to improve memory bandwidth utilization
// Unroll loops further if registers and shared memory limits allow
```