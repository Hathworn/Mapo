```
// Ensure data alignment and memory coalescing for d_r, d_g, d_b
// Precompute constant coefficients to reduce redundant calculations
// Consider using shared memory if multiple threads access the same data
// Use 16-byte aligned memory loads for better memory throughput
```