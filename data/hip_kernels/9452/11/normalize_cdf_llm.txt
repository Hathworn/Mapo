```
// Ensure coalesced memory access by loading contiguous blocks of data
// Use shared memory if possible to reduce global memory traffic
// Consider loop unrolling if computation within loop can be repeated
// Align memory accesses to improve load/store efficiency
```