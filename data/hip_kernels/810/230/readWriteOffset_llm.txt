// Consider memory coalescing by ensuring that data accesses are contiguous
// Use shared memory if there are frequent access patterns that can benefit from less global memory traffic
// Ensure offset does not lead to bank conflicts in memory accesses