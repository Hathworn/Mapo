```
// Simplify index calculation to improve access pattern
// Use a local variable for accumulation to reduce global memory access
// Accumulate directly into local variable to minimize read-modify-write cycles
// Store the scaled result directly to reduce overhead
```