```
// Use blockDim.x * gridDim.x for efficient grid-stride looping
// Ensure memory access is coalesced for X and Y arrays
// Consider using shared memory if there are common reusable values in X or Y
```