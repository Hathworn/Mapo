```cpp
// Use a single linear index to enhance coalesced memory access
// Consider leading dimensions to optimize for aligned memory accesses
// Minimize global memory accesses by reusing data within shared memory when possible
```