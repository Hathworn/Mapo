```
// Use shared memory to reduce global memory accesses
// Coalesce memory accesses for coords array
// Pre-compute multiplications like `x * y` and `2 * x * y * z` to avoid repeated calculations
```