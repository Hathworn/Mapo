```cpp
// Combine condition checks to reduce computational overhead
// Minimize use of conditionals inside kernel for better performance
// Ensure memory accesses are coalesced for better memory throughput
// Precompute offset indices outside the main condition if possible
```