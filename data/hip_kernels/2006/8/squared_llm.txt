// Ensure memory coalescing for result array writes
// Utilize shared memory if frequently accessed elements can benefit from caching
// Minimize conditional checks inside the loop for better performance
// Consider loop unrolling for further improvements in execution speed