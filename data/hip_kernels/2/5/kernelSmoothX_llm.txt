// Consider using shared memory for neighboring indices to reduce global memory access
// Optimize memory access patterns by coalescing if possible
// Ensure memory accesses are aligned for better performance
// Avoid bank conflicts in shared memory by ensuring indices are aligned