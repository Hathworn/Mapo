```cpp
// Use grid-stride loop for better memory access coalescing
// Ensure in0, in1, and out are aligned to improve memory bandwidth
// Consider using shared memory if further optimization is needed
```