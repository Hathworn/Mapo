```
// Use shared memory to improve access speed if multiple threads access the same array values
// Coalesce memory accesses in vertexArray and neighbourArray to improve memory bandwidth utilization
// Minimize conditional checks by restructuring the loop if possible for better warp efficiency
```