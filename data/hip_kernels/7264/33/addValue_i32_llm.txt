// Consider using shared memory if vector data has localized access pattern
// Ensure memory accesses are coalesced to improve bandwidth usage
// Use const restrict qualifiers in parameters to help compiler for better optimizations