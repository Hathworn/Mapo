```
// Use shared memory for data frequently accessed by multiple threads
// Reduce global memory accesses by caching data in registers
// Minimize bank conflicts in shared memory accesses
// Coalesce memory accesses to maximize memory throughput
```