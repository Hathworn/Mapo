```
// Use shared memory for better performance if multiple threads access the same data
// Reduce global memory access by storing in a register
// Reduce global memory access by storing in a register
```