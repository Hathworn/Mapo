```
// Precompute indices to avoid redundant calculations
// Ensure memory accesses are coalesced by accessing consecutive memory locations
// Minimize divergent branching by reordering conditional checks
// Use shared memory to temporarily store data2 for faster access within a block
```
