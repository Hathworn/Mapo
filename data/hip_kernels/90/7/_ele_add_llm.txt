```cpp
// Consider coalescing memory accesses for improved performance
// Use the __restrict__ qualifier if m and target do not alias
// Evaluate if shared memory can provide any benefits in this context
// Ensure memory accesses are 32-byte aligned for optimal performance
```