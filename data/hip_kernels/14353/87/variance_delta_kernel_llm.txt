```cpp
// Precompute var_scale before the loop to optimize performance
// Use local variable 'sum' for better caching and reduced memory access
// Consider unrolling the inner loop to improve memory access patterns
// Write result to 'variance_delta[i]' after loop to combine computations
```