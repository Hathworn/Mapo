```c
// Calculate a linear index to avoid repeated calculations and improve efficiency
// Ensure memory accesses are coalesced to improve global memory throughput
// Use boundary checks to prevent out-of-bounds memory access and improve safety
```