```cpp
// Consider coalesced memory access for score array
// Minimize global memory accesses by maximizing shared memory usage
// Ensure memory access patterns are optimal within the thread block
// Reduce divergence by aligning condition checks across threads
```