// Coalesce memory accesses for better performance by ensuring consecutive threads access consecutive memory locations.
// Minimize the use of printf within kernel functions as it may hinder performance.