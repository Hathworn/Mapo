// Consider using shared memory for frequently accessed data to reduce global memory accesses.
// Ensure memory accesses are coalesced to improve bandwidth utilization.
// Check if loop unrolling could provide performance benefits.
