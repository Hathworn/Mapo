```c
// Use shared memory to reduce global memory accesses
// Exit early if thread index is out of range to save resources
// Store pivot in a register to optimize access times
// Balance subarray processing by handling larger subarrays first
```