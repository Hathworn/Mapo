// Consider using shared memory for faster access to input data.
// Use loop unrolling for better performance if the computation is repetitive.
// Ensure that memory accesses are coalesced for efficient data transfer.