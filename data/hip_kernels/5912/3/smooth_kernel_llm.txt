```
// Pre-calculate indices to reduce redundant computations inside the loops
// Consider using shared memory if multiple threads access nearby data
// Use loop unrolling for better performance in the inner loops
```