```cpp
// Ensure coalesced global memory access for A and B
// Minimize global memory accesses by combining read/write operations
// Consider using shared memory if multiple threads access the same element
```