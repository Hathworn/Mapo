```
// Consider using shared memory to reduce global memory accesses.
// Reuse the calculation of __powf(a, 2.0f) and avoid redundant computations.
// Ensure global memory writes are coalesced where possible.
```