#include "hip/hip_runtime.h"
#include "includes.h"

__global__ void profileSubphaseSmootherSetup_kernel() {
    // Original kernel is empty, consider optimizing
    // by implementing meaningful functionality if needed.
    // Otherwise, an empty kernel has no execution overhead.
}