// Use global linear index to optimize memory coalescing
// Check bounds before memory operations to prevent out of bounds access
// Directly access memory via linear index to minimize memory divergence