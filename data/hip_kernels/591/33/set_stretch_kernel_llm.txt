```
// Ensure coalesced memory accesses by organizing data access sequentially
// Minimize memory accesses by storing 'mean' in a register instead of loading each time
// Use appropriate block and grid sizes to maximize occupancy and throughput
```