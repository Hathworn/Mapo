// Use shared memory if possible to reduce global memory access
// Consider using half-precision floats if accuracy allows for reduced memory usage
// Ensure coalesced memory access for loc_ to improve performance