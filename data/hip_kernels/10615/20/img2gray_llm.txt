```cpp
// Ensure global memory accesses are coalesced
// Calculate the index before conditional checks to reduce redundant calculations
// Use registers to hold intermediate values for the RGB components
// Consider using shared memory for the pixel data if further optimization is needed
```