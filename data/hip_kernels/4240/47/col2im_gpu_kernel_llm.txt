```cpp
// Ensure coalesced memory access by aligning data_col accesses
// Minimize divergent branches within the loops
// Consider using shared memory to cache frequently accessed data_col values
// Precompute constants outside the main loop to reduce computational overhead
```