```
// Use shared memory to store portions of `in` and `factor` if possible
// Coalesced memory accesses: Ensure that global memory reads are coalesced
// Prefetch data into registers to hide memory latency if applicable
// Minimize the number of global memory accesses
```