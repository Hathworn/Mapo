```c
// Optimize global memory access by coalescing reads and writes
// Minimize divergent branches in conditional statements
// Use shared memory for frequently accessed data within blocks
// Prefetch lens data to reduce global memory latency
// Unroll loops if beneficial to performance
```