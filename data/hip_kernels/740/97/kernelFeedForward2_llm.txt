```cpp
// Consider coalesced memory access for zs, biases, and activations
// Utilize shared memory to reduce global memory accesses if possible
// Use warp-level operations for efficient parallel reductions
```