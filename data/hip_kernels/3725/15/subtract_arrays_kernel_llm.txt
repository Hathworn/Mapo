// Using a single global index calculation
// Check if index is within bounds and perform operation
// Restrict pointers to avoid aliasing and improve access speed
// Use const where possible to enable compiler optimizations
// Minimize the use of shared memory and bank conflicts